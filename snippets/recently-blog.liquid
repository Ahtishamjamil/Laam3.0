{% for article in blogs.blog-name.articles %}
    {% if forloop.last != true %}
    <div class="article">
      <h3>{{ article.title }}</h3>
      <div class="article-body textile">
        {{ article.content }}
      </div>
    </div>
    {% endif %}
    {% endfor %}